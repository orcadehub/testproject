1.create github account
2.install git 

Github connection in Terminal

setup configuration ( 1 time )
1.git config user.name "Work Name"
  git config user.email "work@example.com"

To connect local folder with github ( only once for a repo)
1.git init 
2.git remote add origin https://github.com/orcadehub/testproject.git
3.git branch -M main

To push the data to github
1.git add .
2.git commit -m "update 1.0.0"
3.git push origin -u main


To deploy this project 
-> example platforms ( netlify, vercel, render, railway, heroku ..etc)



